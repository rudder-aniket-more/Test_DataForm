config {
  type: "assertion",
  schema: "Aniket_Mydataset",
  name: "assert_id_unique"
}

-- Assertion 1: Check if `id` is unique
SELECT
  id,
  COUNT(*) AS cnt
FROM
  Aniket_Mydataset.student_table
GROUP BY
  id
HAVING
  COUNT(*) > 1

